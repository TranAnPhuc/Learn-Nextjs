// Name export - xuat nhieu thu tu 1 file
export const cong = (a, b) => a + b;
export const tru = (a, b) => a - b;
export const PI = 3.14159;

// Default export - moi file chi co 1 cai
export default function nhanDoi(so) {
  return so * 2;
}
